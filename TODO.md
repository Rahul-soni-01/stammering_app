# TODO: Implement Admin Login Redirect to Admin Dashboard

- [ ] Modify the `store` method in `AuthenticatedSessionController.php` to check if the logged-in user is an admin and redirect to 'admin.dashboard' if true, otherwise to the intended route.
- [ ] Test the login functionality for both admin and regular users to ensure correct redirection.
